name: Publish to npm

on:
  push:
    tags:
      - 'v*.*.*'  # 匹配版本号格式的 tag，如 v2.0.6
  release:
    types: [created]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 2.0.1)'
        required: true
        type: string

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    # 发布凭据：仓库 Secret NPM_TOKEN。请使用 Granular Access Token
    # （限定 pixivflow 包、Read and write 权限）；classic token 自 npm
    # 2025 年末政策起对已有包的发布受限。详见 docs/RELEASING.md 故障诊断节。
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test
        continue-on-error: true  # 如果测试失败也继续（可选，根据需求调整）

      - name: Build project
        run: npm run build

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" == "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
            # 移除 'v' 前缀（如果有）
            VERSION=${VERSION#v}
          elif [ "${{ github.event_name }}" == "push" ]; then
            # 从 tag ref 中提取版本号，格式：refs/tags/v2.0.6
            VERSION="${{ github.ref }}"
            VERSION=${VERSION#refs/tags/v}
            VERSION=${VERSION#refs/tags/}
          else
            VERSION="${{ github.event.inputs.version }}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"

      - name: Check current package.json version
        id: current_version
        run: |
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "current_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "Current package.json version: $CURRENT_VERSION"
          echo "Target version: ${{ steps.version.outputs.version }}"

      - name: Update package.json version
        if: steps.current_version.outputs.current_version != steps.version.outputs.version
        run: |
          npm version ${{ steps.version.outputs.version }} --no-git-tag-commit
          cat package.json | grep '"version"'

      - name: Check if version already published
        id: check_version
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          if npm view pixivflow@$VERSION version &>/dev/null; then
            echo "already_published=true" >> $GITHUB_OUTPUT
            echo "⚠️  Version $VERSION is already published on npm"
          else
            echo "already_published=false" >> $GITHUB_OUTPUT
            echo "✅ Version $VERSION is not published, will proceed"
          fi

      - name: Publish to npm
        if: steps.check_version.outputs.already_published == 'false'
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Skip publish (already exists)
        if: steps.check_version.outputs.already_published == 'true'
        run: |
          echo "⏭️  Skipping publish - version ${{ steps.version.outputs.version }} already exists on npm"
          echo "This is expected if the version was published manually or by another workflow"

      - name: Commit version update back to GitHub
        if: steps.current_version.outputs.current_version != steps.version.outputs.version && (github.event_name == 'release' || github.event_name == 'push')
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json
          if [ -f package-lock.json ]; then
            git add package-lock.json
          fi
          git commit -m "chore: sync version to ${{ steps.version.outputs.version }} [skip ci]" || echo "No changes to commit"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git push origin HEAD:${{ github.ref_name }} || echo "Failed to push version update"

      - name: Create Git tag (workflow_dispatch only)
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "v${{ steps.version.outputs.version }}" -m "Release v${{ steps.version.outputs.version }}"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git push origin "v${{ steps.version.outputs.version }}"

      - name: Verify npm publication
        run: |
          echo "Verifying npm publication..."
          sleep 5  # Wait for npm index to update
          VERSION="${{ steps.version.outputs.version }}"
          PUBLISHED_VERSION=$(npm view pixivflow@$VERSION version 2>/dev/null || echo "")
          if [ "$PUBLISHED_VERSION" == "$VERSION" ]; then
            echo "✅ Version $VERSION verified on npm"
            if [ "${{ steps.check_version.outputs.already_published }}" == "true" ]; then
              echo "ℹ️  Version was already published (skipped publish step)"
            else
              echo "✅ Version was just published by this workflow"
            fi
          else
            echo "⚠️  Version verification pending (may take a few minutes for npm index to update)"
            echo "Expected: $VERSION"
            echo "Found: ${PUBLISHED_VERSION:-not found}"
          fi

      - name: Summary
        run: |
          if [ "${{ steps.check_version.outputs.already_published }}" == "true" ]; then
            echo "## ⏭️  Version Already Published" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Version ${{ steps.version.outputs.version }} was already published on npm." >> $GITHUB_STEP_SUMMARY
            echo "Publish step was skipped." >> $GITHUB_STEP_SUMMARY
          else
            echo "## ✅ Published to npm" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Version**: ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Package**: pixivflow" >> $GITHUB_STEP_SUMMARY
          echo "- **Registry**: https://www.npmjs.com/package/pixivflow" >> $GITHUB_STEP_SUMMARY
          echo "- **GitHub**: https://github.com/redtidev1918/PixivFlow" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 Sync Status" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ package.json: ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ npm: ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ GitHub tag: v${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY

